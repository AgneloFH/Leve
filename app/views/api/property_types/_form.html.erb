<%= form_with(model: @property_type, url: api_property_types_path) do |form| %>
  <% if @property_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property_type.errors.count, "error") %> prevented this property type from being saved:</h2>
      <ul>
        <% @property_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit 'Save' %>
  </div>
<% end %>
